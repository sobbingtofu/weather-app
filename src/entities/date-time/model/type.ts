export type TimeThemeType = "dawn" | "morning" | "noon" | "twilight" | "night";

export interface TimeThemeStyles {
  bg: string;
  cardBg: string;
  text: string;
  link: string;
  footer: string;
}
